# Weather App Thing

A small little web app I made to learn Flask. The UI is kinda eh (I should probably fix it) but it works for now. It's basically just a wrapper for an API call, but it looks kinda nice I guess.
